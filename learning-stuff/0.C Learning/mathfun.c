#include <math.h>
#include <stdio.h>

int main() {
  int x = 9;
  // x = powf(x, 2.0);
  //  x = round(x);
  //  x = ceil(x);
  //  x = floor(x);
  //  x = sqrt(x);
  //  x = abs(x); will give a distance fromzero of a integer
  //  x = log(x); natural logarithm
  //  x = sin(x); x will be in radians
  //  x = cos(x); x will be in radians
  //  x = tan(x;
  //
  printf("\n%d\n", x);
  return 0;
}
